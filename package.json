{
  "name": "thisjs",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "npm-run-all -p build:**",
    "build:js": "babel ./devCode/ --presets es2015 --watch --out-dir ./devCode_compiled/",
    "build:md": "gulp concat && gulp sass && gulp watch",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.6.5",
    "babel-preset-es2015": "^6.6.0",
    "gulp": "^3.9.1",
    "gulp-concat": "^2.6.0",
    "gulp-ruby-sass": "^2.0.6"
  }
}
